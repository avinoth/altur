<%= form_for([@question, @question.answers.build]) do |f| %>
  <%= f.label :name %><br>
  <%= f.text_field :name %><br>
  <br>
  <%= f.label :response %><br>
  <%= f.text_area :response %><br>
  <br>
  <%= f.submit %>
<% end %>